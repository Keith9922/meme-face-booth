# models/ 和 vendor/ 共 51MB，曾经排除在外（跑 setup.sh 拉回来）。
# 但项目接了 GitHub 自动部署后这个判断就不成立了：每次 push 都会生成一个
# 没有模型的空壳部署去覆盖线上。改成直接提交，clone 下来即可运行。
# setup.sh 保留，仍可用于更新版本。

# 表情包素材不进仓库：
#   仓库里那批是从公开仓库借来做技术验证的网络 meme，有版权，不能随仓库分发。
#   正式使用请放自己有授权的图，并在 memes/manifest.json 里登记。
# memes/ 现在全是 CC0 / 公有领域素材（Openverse 采集），可商用可分发，进仓库。
# 有版权的演示图已移到 demo-memes-copyrighted/，只留本地。

.DS_Store
node_modules/
*.log
.vercel
demo-memes-copyrighted/
harvest/
