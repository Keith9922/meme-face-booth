# 可复现的大文件，不进仓库 —— 跑 ./setup.sh 拉回来
models/
vendor/

# 表情包素材不进仓库：
#   仓库里那批是从公开仓库借来做技术验证的网络 meme，有版权，不能随仓库分发。
#   正式使用请放自己有授权的图，并在 memes/manifest.json 里登记。
memes/*
!memes/manifest.json

.DS_Store
node_modules/
*.log
.vercel
